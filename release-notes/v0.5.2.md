# Mindflow v0.5.2

**Released:** 2026-05-04

## Fixes
- Landing page Claude Code config snippet now shows `"mindflow"` (was `"meetflow"` from a missed rebrand sweep).
- Mic-permission-denied error message and VAD-model-missing error now say "Mindflow" instead of the legacy product name.
- Bundled MCP server description, file-IPC docstring, and audio-process comment refer to `Mindflow.app` instead of `Meetflow.app`.
- Browser-extension experiment (`extension/`) renamed in `manifest.json` and console logs.

No functional changes — strings only.
